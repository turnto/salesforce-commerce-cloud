<isif condition="${dw.system.Site.getCurrent().getCustomPreferenceValue('turntoTeasersEnabled')}">
    <iscomment>TurnTo Teasers</iscomment>
	<div id="tt-teaser-widget"></div>
    <script>
        TurnToCmd('initialize', 'teaser'); // when selecting a variant, this template reloads so widget must be re-initialized
    </script>
</isif>
